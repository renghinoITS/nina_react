import useMQTT from "../../hooks/useMQTT";
import React, { useState } from "react";

const App = () => {
    const { messages } = useMQTT();


    return (
        <div>
        </div>
    );
};

export default App;
